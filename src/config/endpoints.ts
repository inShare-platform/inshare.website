import type { endpointsI , apiEnhancement } from "../types/endpoints.types"

const apis : apiEnhancement = {
   pricing:'123',
   auth: '123'
}

export const baseUrls  = {
    main:'https://jsonplaceholder.typicode.com'
}